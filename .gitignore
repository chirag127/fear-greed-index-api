node_modules/
.wrangler/
.dev.vars
.env
dist/
*.log
.DS_Store

# env (synced via sops + age)
.env.local
.env.*.local
.sops-age-key.txt

# Raw NSE archive downloads cached by scripts/backfill-nse.mjs. ~40MB of CSV that
# is fully reproducible from the archive; never commit it.
.cache/
